# 4.1 稳定性架构

## 训练前防线

`audit_training_pipeline` 对历史数据执行有效性、重复期号、时间顺序和特征前缀不变性检查。前缀不变性用于验证：加入未来开奖后，之前已经生成的训练特征和标签不能发生变化。

## 校准与最终训练

机器学习模型先按时间切分训练集和验证集，在验证集上拟合 Platt 校准器并比较校准前后 Brier。只有校准不变差时才启用。随后最终模型使用全部已知历史标签训练，目标期本身不参与训练。

## 模型版本

模型版本由模型名、前后区、数据指纹、树数量、随机种子和校准开关确定。完全一致时直接读取缓存；历史数据增加后生成新指纹并训练新版本。固定历史版本属于显式回滚，恢复自动模式后重新按当前指纹选择模型。

## 数据库保护

SQLite 采用 WAL。备份通过 SQLite Backup API 创建，并对备份执行 `PRAGMA quick_check`。自动备份默认每天最多一次，默认保留10份。

## 异常恢复

所有未处理异常写入 `%LOCALAPPDATA%\\DLTAnalyzerPro2\\crash_reports`。模型文件加载失败时移动到隔离目录，不再参与自动选择。
