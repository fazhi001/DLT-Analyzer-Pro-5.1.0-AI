# DLT Analyzer Pro 5.0 三种彩票可信分析版 — 交付状态

交付日期：2026-07-24

## 本次升级基线

本项目直接基于用户提供的 `DLT-Analyzer-Pro-4.2.3-AI` 原始 GitHub 源码升级，不是另起炉灶，也不是从 EXE 反编译替代。

## 已完成

- 完整保留原大乐透数据、官方更新、统计、AI、可信评估、回测、导入导出与备份逻辑。
- 新增“排列三 / 排列五中心”。
- 新增主界面“一键同步三种彩票”。
- 排列五从体彩数据源执行全量或增量同步。
- 排列三由同期开奖的排列五前三位生成，并独立存储。
- 排列三使用百位、十位、个位 3 个独立位置模型。
- 排列五使用万位、千位、百位、十位、个位 5 个独立位置模型。
- 两种彩票共用特征、训练、验证和预测代码，但数据、预测记录及模型文件相互隔离。
- 位置模型必须在按时间顺序划分的样本外验证中优于统计基线，否则自动回退统计模型。
- 新增排列数据 CSV/Excel 导入、Excel 导出、多注生成、位置分析和滚动验证。
- 原大乐透数据库表不变，升级不会要求重新导入已有大乐透数据。
- Windows PyInstaller、Inno Setup 和 GitHub Actions 构建配置已升级到 5.0.0。
- 推送到 GitHub `main` 分支后会自动运行测试并构建安装包，也可手动运行工作流。

## 验证结果

- 自动测试：46/46 通过。
- 源码自检：通过，输出 `SELFTEST_OK`。
- 稳定性审计：通过。
- 内置大乐透数据：833 期，关键错误 0，警告 0。
- 图形界面启动检查：通过。
- 三种彩票数据库隔离、官方数据解析、前导零、模型位置隔离、号码生成和滚动验证：通过。
- 合成数据集成演练：排列三、排列五各 160 期，训练、生成、回测和 Excel 导出均通过。

测试过程中出现的 LightGBM 特征名提示为第三方库警告，不影响测试结论。

## 当前环境限制

当前执行环境不是 Windows，且本地软件源无法取得指定 PyInstaller 包，因此这里没有伪称已经生成并运行 Windows 安装包。

项目已包含 Windows 云端构建工作流。将源码提交到 GitHub 后，GitHub 的 Windows Runner 会依次执行：

1. 安装 Python 3.12 和依赖；
2. 运行 46 项测试、自检及稳定性审计；
3. 构建桌面程序；
4. 启动检查打包后的 GUI；
5. 生成安装包和便携版；
6. 生成 SHA-256 校验文件。

预期安装包名称：

`DLT_Analyzer_Pro_5.0.0_3Games_Setup_x64.exe`

## 使用边界

彩票开奖结果具有随机性。程序用于历史数据分析、模型验证和娱乐研究，不保证中奖。
