# DLT Analyzer Pro 4.2.2 北京时间与记录管理版

## 修复

- 修复预测记录时间直接显示 SQLite UTC 时间、与北京时间相差 8 小时的问题。
- 所有旧预测记录无需迁移，显示时自动转换为北京时间。

## 新增

- 主界面顶部实时显示北京时间。
- 预测记录支持按目标期号筛选。
- 预测记录支持按最低、最高相对综合分筛选。
- 支持 Ctrl/Shift 多选后删除预测记录。
- 支持清空全部预测记录，并提供二次确认。
- 删除前自动生成数据库校验备份。
- 删除操作不会影响开奖历史、模型版本、设置和已有备份。

## 术语调整

- “AI评分”统一更名为“相对综合分”。
- 相对综合分仅用于本次候选组合之间的排序，不是中奖概率。

## 数据安全

数据库仍以 UTC 保存时间，界面统一转换为 UTC+8 北京时间。这样可避免系统时区变化导致历史记录时间漂移。

### 实时北京时间增强

- 北京时间每秒实时走秒，不再是页面打开时的静态时间。
- 启动后后台使用中国体彩网 HTTP 响应时间进行网络校时。
- 每 30 分钟自动重新校时，网络失败时继续使用本机时钟，不影响预测和数据查询。
- 界面明确显示“网络校时”或“本机时钟”，便于判断时间来源。
- 校时后使用单调时钟持续推进，避免 Windows 时区或手工改时造成显示跳变。
