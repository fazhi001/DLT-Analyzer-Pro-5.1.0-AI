# 大乐透 AI 预测系统技术设计

## 1. 数据边界

系统默认使用最近五年历史数据。存在开奖日期时按日期回溯五年；旧数据库尚未补齐日期时，使用最近 850 期作为兜底。所有机器学习特征只能读取目标期之前的数据。

## 2. 概率组件

### 贝叶斯更新

以前区理论出现率 5/35、后区理论出现率 2/12 作为先验均值，结合指数衰减历史观测计算 Beta-Bernoulli 后验期望。

### 马尔可夫链

对每个号码构建“上期未出现/出现”两个状态，估计状态到下一期出现的条件概率，并使用拉普拉斯平滑。

### 遗漏周期

从号码历史出现位置计算间隔分布，根据当前遗漏长度估算下一期出现的经验风险率，同时加入相对平均间隔的逾期程度。

### 机器学习

XGBoost 与 LightGBM 均执行号码级二分类。每期开奖、每个候选号码构成一条样本，目标为该号码在下一期是否出现。

## 3. 集成

六个组件均归一化后加权：

- 贝叶斯
- 马尔可夫
- 遗漏周期
- 近期频率
- XGBoost
- LightGBM

默认权重可由自动优化模块覆盖。

## 4. 蒙特卡洛

采用加权无放回抽样，以指数竞赛方法批量生成组合。默认模拟 1,000,000 次，可设置为 1,500,000、2,000,000 或 3,000,000 次。

组合评价同时考虑：

- 集成号码概率
- 前区和值历史分布
- 奇偶结构
- 三分区结构
- 连号概率
- 后区跨度

## 5. 遗传算法

蒙特卡洛高分组合作为初始种群，执行：

- 锦标赛选择
- 父代号码池交叉
- 加权突变
- 精英保留
- 组合修复与去重

最终再执行组合间重叠惩罚，避免输出高度相似的多注号码。

## 6. 回测与优化

使用滚动前向验证：每个回测点只使用此前数据训练，预测下一期。评估指标包括：

- 前区平均命中
- 后区平均命中
- 随机基线平均命中
- 前区 Brier 分数
- 后区 Brier 分数

参数优化对六模型权重进行 Dirichlet 随机搜索，目标函数同时考虑命中与概率校准。
