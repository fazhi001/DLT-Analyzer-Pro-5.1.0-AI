# DLT Analyzer Pro 4.2.3 可信基线保护版

本版不新增预测模型，专门修复可信回测发现的方向判断和集成风险。

## 核心修复

- 修正负 BSS 的结论文字：模型 Brier 高于基线时明确显示“模型劣于均匀概率基线”。
- 滚动验证期数不再被静默限制为 20 期，现支持 3～100 期。
- 前区和后区使用独立动态权重，不再强制共用同一组权重。
- 动态权重目标以样本外 Brier 为主，命中数仅用于极小比例的同分判断。
- 加入均匀概率基线保护：
  - BSS 95% 区间完全低于 0 时，概率层自动回退到均匀基线；
  - BSS 不显著时，模型概率自动向基线收缩；
  - 保护后的验证 Brier 不允许高于基线。
- “一键自适应预测”和“可信回测”都应用相同的前后区权重及基线占比。
- 模型中心显示前区、后区模型概率占比和基线占比。
- Excel 报告增加前区权重、后区权重和基线保护信息。

## 说明

基线保护只能防止概率层在验证窗口内明显劣于均匀概率基线，不能保证未来中奖，也不能将彩票变成可稳定预测事件。
