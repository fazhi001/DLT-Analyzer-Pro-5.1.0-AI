# DLT Analyzer Pro 4.1 AI 稳定性版

版本：4.1.0

## 新增

1. 时间序列数据泄漏审计与训练阻断。
2. XGBoost、LightGBM 独立验证集 Platt 概率校准。
3. 校准质量保护：Brier 变差时自动退回未校准概率。
4. 全流程固定随机种子与 LightGBM 确定性参数。
5. SQLite quick_check、每日自动备份、备份校验、保留策略和恢复接口。
6. 模型数据指纹、版本清单、缓存复用、增量重训练和历史版本固定。
7. 损坏模型自动隔离，特征结构不兼容时自动解除固定版本并重新训练。
8. 主线程、工作线程和 Tk 回调崩溃报告。
9. “稳定性中心”页面：检查、备份、模型版本列表、固定版本、恢复自动最新。
10. GitHub Actions 在打包前执行单元测试、自检和稳定性审计。

## 验证

- 26 项自动化测试通过。
- SELFTEST_OK。
- 833 期内置历史数据稳定性检查通过，严重问题 0、警告 0。
- 一键自适应流程完成：动态回测、100 万次蒙特卡洛、5 注输出、数据库备份、4 个机器学习模型版本落盘。
- 第二次评分成功命中 XGBoost/LightGBM 前后区共 4 个模型缓存。

## 注意

当前环境完成了 Linux 源码级测试。Windows EXE 和 Inno Setup 安装包仍需由 GitHub Actions 的 windows-latest 实际构建后验收。
